# Foundry
**/out/
**/cache/
**/broadcast/

# Node
**/node_modules/
**/npm-debug.log*

# Editor / IDE / agent
.vscode/
.idea/
.claude/
*.swp

# Local-only audit + working notes (never committed)
Working Notes/

# OS
.DS_Store
Thumbs.db

# Syncthing
.stfolder/
.stversions/
.stignore

# Secrets
.env
.env.*
!.env.example

# LaTeX build artefacts
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg

# Reproduction code and datasets held back pending verification.
# (pcs/ and slipstream/ are already tracked in HEAD; gitignore here only
# suppresses NEW files inside them. Existing tracked files in those
# directories stay where they are; explicit modifications won't be staged
# as part of paper-only revisions.)
quickswap/
ramses/
shadow/
scripts/
citations/
